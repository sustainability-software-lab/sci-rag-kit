A user asked this question about $DOMAIN_NAME:

"$QUERY"

List the specific entities the question mentions or clearly implies, so we can look them up in a knowledge graph. Prefer canonical short names. Entity types in this domain include:

$ENTITY_TYPES

Respond with JSON only:

{"entities": ["name one", "name two"]}

If the question names no specific entities, respond with {"entities": []}.
